#include <stdio.h>

int main() {
    char *will_never_hear_again = "Hey Q, when is the memory course in C gonna be done?";

    // multi variable declaration
    char* p1 = "Yes!", *p2 = "No!";
    char *q1, q2;

    // don't touch this line below
    printf("%s\n", will_never_hear_again);
    return 0;
}